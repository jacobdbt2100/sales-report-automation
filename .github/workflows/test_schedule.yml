name: Schedule Test

on:
  workflow_dispatch:

  schedule:
    - cron: "3-59/5 * * * *"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Display test message
        run: echo "Scheduled workflow is working!"
